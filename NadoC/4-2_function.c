#include <stdio.h>

int main(void) // int = 반환형 main = 함수 (전달값)
{
// 함수의 형태 //
// '반환형 함수이름(전달값)' 의 형태
// 'return type function name (delivery value?? = parameter or argument)'
// (전달값 중에 매개변수가 있는듯하다)

// ex)
// 전달값 : 36
// 함수 : ㅁ + 4
// 반환형 : ?

int 함수이름 (num) // double, float, char,void = 반환하지 않을때
{
    return num + 4;
}

//이런 함수들을 작성할때 위쪽에 작성한 선언과 완전히 동일해야한다. 

int 함수이름 (int num1, int num2, char c, float f) // 여러개 가능
{
    return num + 4;
}
    return 0;
}

